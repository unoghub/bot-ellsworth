services:
  bot:
    build: .
    container_name: unog-bot
    restart: unless-stopped
    network_mode: host
    environment:
      - TOKEN=${TOKEN}
      - CLIENT_ID=${CLIENT_ID}
      - GUILD_ID=${GUILD_ID}
    volumes:

      - ./data:/app/data